language: ruby
rvm:
  - jruby-18mode
jdk:
  - oraclejdk7
before_install:
  - gem update --system
  - gem --version
install:
  bundle install
script:
  bundle exec buildr clean test
after_success:
  bundle exec buildr package test=no
env:
  - JRUBY_OPTS='-J-XX:MaxPermSize=512m'